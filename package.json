{
  "name": "uniac_player",
  "version": "0.0.1",
  "description": "Song-queueing web interface for home hardware",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "rm -rf dist/* && tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.10",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "tslib": "^1.10.0"
  },
  "author": "Michael Eaton <byzanitnefailure@gmail.com>",
  "license": "MIT"
}
